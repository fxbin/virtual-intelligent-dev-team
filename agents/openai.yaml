interface:
  display_name: "Virtual Intelligent Dev Team"
  short_description: "Route expert teams with planning, iteration, and release gates."
  default_prompt: "Use $virtual-intelligent-dev-team to auto-route this request, enter a lightweight pre-development planning branch for large rewrites or migrations, preserve the semantic lead, and run bounded self-optimization with baseline registration, round memory, self-feedback, context-chain updates, rollback decisions, and distilled-pattern sync when optimization loops are needed."
